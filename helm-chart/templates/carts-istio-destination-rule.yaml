apiVersion: networking.istio.io/v1alpha3
kind: DestinationRule
metadata:
  name: carts-floshop-production-destination
spec:
  host: carts.floshop-production.svc.cluster.local
  subsets:
  - name: blue
    labels:
      deployment: carts-blue
  - name: green
    labels:
      deployment: carts-green
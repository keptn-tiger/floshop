apiVersion: networking.istio.io/v1alpha3
kind: DestinationRule
metadata:
  name: carts-floshop-staging-destination
spec:
  host: carts.floshop-staging.svc.cluster.local
  subsets:
  - name: blue
    labels:
      deployment: carts-blue
  - name: green
    labels:
      deployment: carts-green